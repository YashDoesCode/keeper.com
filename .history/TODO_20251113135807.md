# Task: Remove Boot Animation, Add Cool Animations to Hero Elements, Move Newsletter to Hero

## Steps to Complete

1. **Remove boot-hidden classes from index.html**
   - Remove all `boot-hidden` classes from elements in the HTML file.
   - This includes header, top-right buttons, hero intro, hero secondary actions, hero metrics, waitlist form, and newsletter form.

2. **Remove boot-animation.js script from index.html**
   - Delete the `<script src="../JavaScript/boot-animation.js"></script>` line from the HTML.

3. **Move newsletter form to hero section**
   - Move the newsletter form from the CTA section to the hero section, placing it after the hero metrics.
   - Ensure it is positioned above the gif background (on top of it).

4. **Update keeper-text-animation.js for new animations**
   - Implement typewriter effect for the hero title ("Encrypted. Empowered. Eternal.").
   - Add staggered fade-in for the subtitle.
   - Animate hero secondary actions buttons sliding in from sides.
   - Animate the moved newsletter form with a bounce fade-in effect.
   - Trigger all animations on DOMContentLoaded.

5. **Test the changes**
   - Load the page to verify boot animation is removed.
   - Check that new animations play correctly.
   - Ensure newsletter is positioned above the gif background.
   - Verify no errors in console.

## Status
- [ ] Step 1: Remove boot-hidden classes
- [ ] Step 2: Remove boot-animation script
- [ ] Step 3: Move newsletter to hero
- [ ] Step 4: Update JavaScript animations
- [ ] Step 5: Test changes
